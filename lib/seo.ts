/**
 * Configuración central de SEO.
 *
 * IMPORTANTE: cambia SITE_URL por tu dominio real antes de desplegar
 * (o defínelo como variable de entorno NEXT_PUBLIC_SITE_URL en Vercel:
 * Project Settings → Environment Variables). Todo el SEO (canonical,
 * Open Graph, sitemap, robots.txt, JSON-LD) depende de este valor.
 */
export const SITE_URL =
  process.env.NEXT_PUBLIC_SITE_URL || "https://TU-DOMINIO.com";

export const SITE_NAME = "Cachirula & Loojan";
