/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Si luego usas imágenes externas (ej. CDN), agrega el dominio aquí.
    remotePatterns: [],
  },
};

module.exports = nextConfig;
